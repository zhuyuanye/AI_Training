# DEV 03｜追踪当前实现：输入输出示例

[对应提示词](../../DEV/03-trace-current-implementation.md) · 实际工作模式：**Plan**

> 静态教学样例。源码路径、版本、评审与结果均为虚构；未修改或运行业务项目。

## 输入示例

输入 DEV 02 地图、DEMO-BASE 的设定与 AC-v1。
教学设定中的修改前行为：非空时做旧格式校验，但新增与编辑都允许空值。

## 输出产物示例

### dev/feature-trace.md

| 链路点 | DEMO-BASE 的教学行为 | 对应目标／差异 |
|---|---|---|
| Vue 新增／编辑 | 已有 email 输入；可空 | AC-01/05 缺必填与清楚提示 |
| Controller | 参数绑定后调用服务 | 不能假定参数绑定已完成必填 |
| Service.create/update | 空值可跳过旧格式检查 | AC-01 的直接接口场景不满足 |
| Repository | 接收服务交付的数据 | 需要验证失败时没有写入 |
| 历史登录／查询 | 不依赖 email 非空 | AC-04 要保持这一点 |

拟核查的字段流：用户输入→表单字段→请求 email→服务规范化→存储→再次查询。
业务未知：完整既有格式／唯一性规则 BLOCK-01。
实现证据限制：以上由 CODE-v1 人工构造，无真实文件行号、搜索结果或运行结果。

## 讲解检查点

阅读当前代码是发现差异，不能把旧行为当成需求正确答案。

[DEV 示例索引](README.md) · [全部示例](../README.md)


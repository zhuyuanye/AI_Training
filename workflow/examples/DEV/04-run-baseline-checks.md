# DEV 04｜检查修改前基线：输入输出示例

[对应提示词](../../DEV/04-run-baseline-checks.md) · 实际工作模式：**Build**

> 静态教学样例。源码路径、版本、评审与结果均为虚构；未修改或运行业务项目。

## 输入示例

输入 DEMO-BASE、CODE-v1 的示意工具链，预设工作区为干净状态（教学设定）。
示例检查：后端已有单元测试、前端构建、后端打包。课堂展示下表，不运行命令。

## 输出产物示例

### dev/baseline-checks.md

**模拟结果，未实际执行。批次 SIM-DEV-BASE-01，代码标签 DEMO-BASE。**

| 检查 | 示例命令／工作目录 | 模拟结果 | 能说明什么 |
|---|---|---|---|
| 旧单元测试 | server：mvn test | 6 项，6 通过；退出码 0 | 旧测试在假设基线上正常 |
| 前端构建 | web：npm run build | 退出码 0 | 能构建，不证明需求满足 |
| 后端打包 | server：mvn -DskipTests package | 退出码 0 | 跳过测试的打包成功 |

已知差异：允许空邮箱是此次要修改的旧行为；旧测试绿色不等于新 AC 已满足。

### dev/runs/SIM-DEV-BASE-01/（应有产物形态）

```text
run.md             # 版本、环境、目录、命令、起止时间、原进程退出码
unit.log           # 原始测试输出
surefire/*.xml     # 框架原始结果
web-build.log      # 前端构建输出
server-package.log # 打包输出
```

上述仅为路径示意，文件没有生成。本次真实执行数为 0；真实运行还应记录改动状态及证据索引。

## 讲解检查点

基线用于区分旧问题与新引入问题。特别指出跳过测试的构建不是测试通过。

[DEV 示例索引](README.md) · [全部示例](../README.md)

